# Prompt Designer Web UI - 设计方案

> **项目目标**：创建一个可视化的 Web 端 UI，用于设计、生成、存储和转换 AI 提示词。

---

## 一、项目概述

### 1.1 核心功能

| 功能模块 | 描述 |
|----------|------|
| **模型 API 接入** | 支持多种第三方 AI 模型 API，用户可自由选择 |
| **CLI 专业提示词生成** | 遵循 Anthropic 官方准则（CLAUDE.md）生成结构化提示词 |
| **Web 端简明提示词生成** | 遵循 Webrule.md 规则生成适用于 Web 对话的提示词 |
| **提示词存储** | 保存用户生成的提示词，支持随取随用 |
| **一键格式切换** | CLI ↔ Web 格式互转，双版本并存 |

### 1.2 技术栈建议

| 层级 | 技术选型 | 备选方案 |
|------|----------|----------|
| **前端框架** | React + TypeScript | Vue 3 + TypeScript |
| **UI 组件库** | Tailwind CSS + shadcn/ui | Ant Design / Material UI |
| **状态管理** | Zustand | Redux Toolkit / Jotai |
| **后端框架** | Node.js + Express | Python + FastAPI |
| **数据库** | SQLite (本地) / PostgreSQL (云端) | MongoDB |
| **API 通信** | RESTful API + WebSocket (流式输出) | tRPC |
| **部署** | Vercel / Docker | Cloudflare Workers |

---

## 二、功能模块详细设计

### 2.1 模型 API 接入模块

#### 2.1.1 支持的模型

| 模型提供商 | 模型 | API 端点 |
|------------|------|----------|
| **Anthropic** | Claude 3.5 Sonnet, Claude 3 Opus | `api.anthropic.com` |
| **OpenAI** | GPT-4o, GPT-4 Turbo | `api.openai.com` |
| **Google** | Gemini 1.5 Pro, Gemini 2.0 Flash | `generativelanguage.googleapis.com` |
| **DeepSeek** | DeepSeek-V3, DeepSeek-R1 | `api.deepseek.com` |
| **自定义** | OpenAI 兼容 API | 用户自定义端点 |

#### 2.1.2 API 配置界面

```
┌─────────────────────────────────────────────────────────────┐
│  ⚙️ 模型配置                                                 │
├─────────────────────────────────────────────────────────────┤
│  选择模型提供商: [Anthropic ▼]                               │
│                                                              │
│  API Key: [••••••••••••••••••••••••]  [👁 显示] [🔄 验证]    │
│                                                              │
│  模型选择: [Claude 3.5 Sonnet ▼]                            │
│                                                              │
│  高级选项:                                                   │
│  ├─ Temperature: [0.7 ────●────── 1.0]                      │
│  ├─ Max Tokens:  [4096        ]                             │
│  └─ Base URL:    [https://api.anthropic.com] (可选)         │
│                                                              │
│  [保存配置]  [测试连接]                                      │
└─────────────────────────────────────────────────────────────┘
```

#### 2.1.3 数据结构

```typescript
interface ModelConfig {
  id: string;
  provider: 'anthropic' | 'openai' | 'google' | 'deepseek' | 'custom';
  apiKey: string;          // 加密存储
  modelId: string;         // e.g., "claude-3-5-sonnet-20241022"
  baseUrl?: string;        // 自定义端点
  temperature: number;     // 0-1
  maxTokens: number;
  isDefault: boolean;
}

interface ApiResponse {
  success: boolean;
  content?: string;
  error?: string;
  usage?: {
    inputTokens: number;
    outputTokens: number;
  };
}
```

---

### 2.2 CLI 专业提示词生成模块

#### 2.2.1 生成流程

```
┌──────────────────────────────────────────────────────────────┐
│  Step 1: 角色描述输入                                         │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ 请描述你想要创建的 AI 角色：                             │  │
│  │                                                          │  │
│  │ 例如：我需要一个能够分析学术论文、提供代码优化建议、     │  │
│  │ 并帮助撰写研究报告的科研助手。需要支持 Python/PyTorch。  │  │
│  │                                                          │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                              │
│  [下一步 →]                                                  │
└──────────────────────────────────────────────────────────────┘
         ↓
┌──────────────────────────────────────────────────────────────┐
│  Step 2: AI 分析与方案生成                                    │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ 🤖 AI 分析结果：                                         │  │
│  │                                                          │  │
│  │ 基于您的描述，我建议采用以下设计方案：                   │  │
│  │                                                          │  │
│  │ 📋 角色定位：科研助手 + 代码专家                         │  │
│  │ 🎯 核心任务：论文分析、代码优化、报告撰写                │  │
│  │ 🛠️ 推荐模板：模板 C (代码/技术任务型) + 模板 E (深度推理)│  │
│  │                                                          │  │
│  │ 📌 建议包含的 XML 标签：                                 │  │
│  │    <role> <task> <thinking> <instructions>               │  │
│  │    <output_format> <constraints> <example>               │  │
│  │                                                          │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                              │
│  [← 返回修改]  [接受并继续 →]  [自定义调整 ⚙️]               │
└──────────────────────────────────────────────────────────────┘
         ↓
┌──────────────────────────────────────────────────────────────┐
│  Step 3: 方案调整（可选）                                     │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ ☑️ 角色定义 (<role>)                                     │  │
│  │ ☑️ 任务声明 (<task>)                                     │  │
│  │ ☑️ 思考框架 (<thinking>)         [展开编辑 ▼]            │  │
│  │ ☑️ 操作指令 (<instructions>)                             │  │
│  │ ☑️ 输出格式 (<output_format>)                            │  │
│  │ ☑️ 约束条件 (<constraints>)                              │  │
│  │ ☐ 示例 (<example>)               [添加示例]              │  │
│  │                                                          │  │
│  │ 语言：[中文 ▼]  输出风格：[专业严谨 ▼]                   │  │
│  │                                                          │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                              │
│  [← 返回]  [生成提示词 →]                                    │
└──────────────────────────────────────────────────────────────┘
         ↓
┌──────────────────────────────────────────────────────────────┐
│  Step 4: 生成结果                                             │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ ## CLI 专业版 (Anthropic XML)                [复制] [下载]│  │
│  │ ┌──────────────────────────────────────────────────────┐ │  │
│  │ │ <role>                                                │ │  │
│  │ │ 你是一位世界顶尖的...                                 │ │  │
│  │ │ </role>                                               │ │  │
│  │ │ ...                                                   │ │  │
│  │ └──────────────────────────────────────────────────────┘ │  │
│  │                                                          │  │
│  │ [🔄 一键转换为 Web 版]                                   │  │
│  │                                                          │  │
│  │ ## Web 简明版 (Gemini/GPTs)               [复制] [下载]  │  │
│  │ ┌──────────────────────────────────────────────────────┐ │  │
│  │ │ 你将扮演'科研助手'，一位世界顶尖的...                 │ │  │
│  │ │ ...                                                   │ │  │
│  │ └──────────────────────────────────────────────────────┘ │  │
│  │                                                          │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                              │
│  [保存到仓库]  [继续优化]  [新建提示词]                      │
└──────────────────────────────────────────────────────────────┘
```

#### 2.2.2 生成系统提示词（内部使用）

AI 生成提示词时，使用以下内部 System Prompt：

````xml
<role>
你是一位专业的 Prompt 设计师，精通 Anthropic 官方提示词设计准则。
你的任务是根据用户的角色描述，生成符合 Anthropic 官方格式的高质量提示词。
</role>

<task>
根据用户提供的角色描述，设计一个完整的 AI 提示词，包含以下 XML 标签结构：
<role>、<task>、<thinking>、<instructions>、<output_format>、<constraints>
可选：<example>、<commands>
</task>

<instructions>
1. 分析用户的角色描述，识别：
   - 核心身份定位
   - 主要任务目标
   - 专业领域和能力要求
   - 输出格式需求

2. 按照以下模板结构生成提示词：
   - <role>：明确 AI 身份、专业背景、核心能力
   - <task>：使用 "你的任务是..." 句式声明核心任务
   - <thinking>：设计内部推理框架（标注"不直接输出"）
   - <instructions>：编写具体操作步骤
   - <output_format>：只写通用格式规范，多场景格式选择放在 instructions
   - <constraints>：语言、态度、格式、底线要求

3. 质量检查：
   - 使用肯定式指令而非否定式
   - 确保 <thinking> 标注为内部推理
   - <output_format> 不包含多个 --- 分隔的模板
</instructions>

<output_format>
## 设计方案分析

**角色定位**：[识别的角色]
**核心任务**：[识别的任务]
**推荐模板**：[模板 A/B/C/D/E]
**建议标签**：[使用的 XML 标签列表]

## 生成的提示词

```xml
<role>
[角色定义内容]
</role>

<task>
[任务声明内容]
</task>

...
```
</output_format>

<constraints>
- 严格遵循 Anthropic 官方提示词设计准则
- XML 标签结构必须完整规范
- 语言：根据用户描述自动判断中英文
- 禁止在 <output_format> 中放置多种格式模板
</constraints>
````

#### 2.2.3 数据结构

```typescript
interface PromptDesignSession {
  id: string;
  userId: string;
  createdAt: Date;
  updatedAt: Date;

  // Step 1: 用户输入
  userDescription: string;

  // Step 2: AI 分析
  aiAnalysis: {
    roleIdentification: string;
    taskGoals: string[];
    recommendedTemplate: 'A' | 'B' | 'C' | 'D' | 'E';
    suggestedTags: string[];
  };

  // Step 3: 用户调整
  userAdjustments: {
    enabledTags: string[];
    language: 'zh' | 'en';
    style: 'professional' | 'friendly' | 'academic';
    customizations: Record<string, string>;
  };

  // Step 4: 生成结果
  generatedPrompt: {
    cliVersion: string;    // Anthropic XML 格式
    webVersion: string;    // Web 端格式
  };
}
```

---

### 2.3 Web 端简明提示词生成模块

#### 2.3.1 生成流程

与 CLI 版本相似，但生成的目标格式不同。

**核心差异：**

| 方面 | CLI 专业版 | Web 简明版 |
|------|-----------|-----------|
| 格式 | Anthropic XML 标签 | 纯文本结构化 |
| 角色 | `<role>...</role>` | `你将扮演'角色名'，...` |
| 任务 | `<task>...</task>` | `目的与目标：` + 星号列表 |
| 思考 | `<thinking>...</thinking>` | 简化为一行，作为行为准则子项 |
| 指令 | `<instructions>...</instructions>` | `行为准则：` + 双层编号 `1) a)` |
| 输出 | `<output_format>...</output_format>` | `输出格式要求：` + 描述 |
| 约束 | `<constraints>...</constraints>` | `语言与态度：` + 性格描述 |
| 示例 | `<example>...</example>` | 通常删除（字符限制） |

#### 2.3.2 转换规则（Webrule.md 核心）

```typescript
interface ConversionRules {
  // 角色转换
  role: {
    pattern: /<role>([\s\S]*?)<\/role>/;
    transform: (content: string) => `你将扮演'${extractRoleName(content)}'，${content}`;
  };

  // 任务转换
  task: {
    pattern: /<task>([\s\S]*?)<\/task>/;
    transform: (content: string) => `目的与目标：\n${toStarList(content)}`;
  };

  // 思考框架转换
  thinking: {
    pattern: /<thinking>([\s\S]*?)<\/thinking>/;
    transform: (content: string) => `X) 内部思考框架 (不直接输出)：\n    - ${simplifyToKeywords(content)}`;
  };

  // 指令转换
  instructions: {
    pattern: /<instructions>([\s\S]*?)<\/instructions>/;
    transform: (content: string) => `行为准则：\n${toNumberedList(content)}`;
  };

  // 输出格式转换
  output_format: {
    pattern: /<output_format>([\s\S]*?)<\/output_format>/;
    transform: (content: string) => `输出格式要求：\n${toDescriptive(content)}`;
  };

  // 约束转换
  constraints: {
    pattern: /<constraints>([\s\S]*?)<\/constraints>/;
    transform: (content: string) => `语言与态度：\n${addPersonality(content)}`;
  };
}
```

---

### 2.4 提示词存储模块

#### 2.4.1 存储结构

```typescript
interface StoredPrompt {
  id: string;
  userId: string;

  // 基础信息
  name: string;                 // 用户命名
  description: string;          // 简短描述
  category: PromptCategory;     // 分类
  tags: string[];               // 标签

  // 提示词内容
  cliVersion: string;           // CLI 专业版 (Anthropic XML)
  webVersion: string;           // Web 简明版

  // 元数据
  createdAt: Date;
  updatedAt: Date;
  usageCount: number;           // 使用次数
  isFavorite: boolean;          // 收藏标记

  // 来源信息
  sourceSession?: string;       // 生成会话 ID
  modelUsed?: string;           // 使用的模型
}

type PromptCategory =
  | 'coding'      // 编程开发
  | 'writing'     // 创意写作
  | 'academic'    // 学术科研
  | 'education'   // 教育学习
  | 'business'    // 商务办公
  | 'translation' // 翻译语言
  | 'analysis'    // 数据分析
  | 'other';      // 其他
```

#### 2.4.2 仓库界面

```
┌─────────────────────────────────────────────────────────────────┐
│  📚 我的提示词仓库                           [+ 新建] [⬆️ 导入]  │
├─────────────────────────────────────────────────────────────────┤
│  🔍 搜索...                    分类：[全部 ▼]  排序：[最近 ▼]   │
├─────────────────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ ⭐ 科研专家助手                               [学术科研]   │  │
│  │ 辅助论文分析、代码优化、研究报告撰写                       │  │
│  │ 📅 2024-01-15  ⟳ 使用 23 次                              │  │
│  │ [CLI版] [Web版] [编辑] [复制] [删除]                      │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │    前端工程师                                 [编程开发]   │  │
│  │ 代码分析、问题诊断、页面布局优化                          │  │
│  │ 📅 2024-01-12  ⟳ 使用 15 次                              │  │
│  │ [CLI版] [Web版] [编辑] [复制] [删除]                      │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ...                                                            │
├─────────────────────────────────────────────────────────────────┤
│  共 12 个提示词  |  导出全部  |  批量管理                       │
└─────────────────────────────────────────────────────────────────┘
```

#### 2.4.3 详情/预览界面

```
┌─────────────────────────────────────────────────────────────────┐
│  科研专家助手                                    ⭐ [编辑] [×]  │
├─────────────────────────────────────────────────────────────────┤
│  分类：学术科研  |  标签：#论文 #代码 #AI                       │
│  创建：2024-01-15  |  最后使用：2024-01-20  |  使用次数：23    │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────────────┐  ┌──────────────────┐                    │
│  │  [CLI 专业版]    │  │  [Web 简明版]    │    ← 标签切换       │
│  └──────────────────┘  └──────────────────┘                    │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ <role>                                                     │  │
│  │ 你是一位世界顶尖的计算机科学与人工智能领域的科研专家。     │  │
│  │ 你具备深厚的学术背景、严谨的科学态度以及极强的工程落地能力。│  │
│  │ ...                                                        │  │
│  │ </role>                                                    │  │
│  │                                                            │  │
│  │ <task>                                                     │  │
│  │ 你的任务是辅助用户进行高水平的学术研究...                  │  │
│  │ </task>                                                    │  │
│  │ ...                                                        │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                 │
│  [📋 复制到剪贴板]  [⬇️ 下载 .md]  [🔄 重新生成]  [🔀 转换格式] │
└─────────────────────────────────────────────────────────────────┘
```

---

### 2.5 一键格式切换模块

#### 2.5.1 转换逻辑

```typescript
// CLI → Web 转换
function cliToWeb(cliPrompt: string): string {
  let result = '';

  // 1. 角色转换
  const roleMatch = cliPrompt.match(/<role>([\s\S]*?)<\/role>/);
  if (roleMatch) {
    const roleName = extractRoleName(roleMatch[1]);
    result += `你将扮演'${roleName}'，${cleanContent(roleMatch[1])}\n\n`;
  }

  // 2. 任务转换
  const taskMatch = cliPrompt.match(/<task>([\s\S]*?)<\/task>/);
  if (taskMatch) {
    result += `目的与目标：\n${toStarList(taskMatch[1])}\n\n`;
  }

  // 3. 指令转换 (包含 thinking)
  const instructionsMatch = cliPrompt.match(/<instructions>([\s\S]*?)<\/instructions>/);
  const thinkingMatch = cliPrompt.match(/<thinking>([\s\S]*?)<\/thinking>/);

  result += '行为准则：\n';
  if (instructionsMatch) {
    result += toNumberedList(instructionsMatch[1]);
  }
  if (thinkingMatch) {
    result += `X) 内部思考框架 (不直接输出)：\n    - ${simplifyToKeywords(thinkingMatch[1])}\n`;
  }
  result += '\n';

  // 4. 输出格式转换
  const outputMatch = cliPrompt.match(/<output_format>([\s\S]*?)<\/output_format>/);
  if (outputMatch) {
    result += `输出格式要求：\n${toDescriptive(outputMatch[1])}\n\n`;
  }

  // 5. 约束转换
  const constraintsMatch = cliPrompt.match(/<constraints>([\s\S]*?)<\/constraints>/);
  if (constraintsMatch) {
    result += `语言与态度：\n${addPersonality(constraintsMatch[1])}\n`;
  }

  return result;
}

// Web → CLI 转换 (反向)
function webToCli(webPrompt: string): string {
  // ... 反向解析逻辑
}
```

#### 2.5.2 转换界面

```
┌─────────────────────────────────────────────────────────────────┐
│  🔄 格式转换器                                                   │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────┐    ┌─────────────────────────┐     │
│  │  📥 输入                 │    │  📤 输出                 │     │
│  │  格式：[CLI XML ▼]      │ → │  格式：[Web 简明 ▼]      │     │
│  ├─────────────────────────┤    ├─────────────────────────┤     │
│  │ <role>                  │    │ 你将扮演'科研专家'，    │     │
│  │ 你是一位世界顶尖的...   │    │ 一位世界顶尖的...       │     │
│  │ </role>                 │    │                         │     │
│  │                         │    │ 目的与目标：            │     │
│  │ <task>                  │    │ * 辅助用户进行...       │     │
│  │ ...                     │    │                         │     │
│  │                         │    │ 行为准则：              │     │
│  │                         │    │ 1) 深度解析：           │     │
│  │                         │    │    a) 对用户上传的...   │     │
│  │                         │    │ ...                     │     │
│  └─────────────────────────┘    └─────────────────────────┘     │
│                                                                 │
│  [📋 粘贴]  [📁 导入文件]        [📋 复制]  [⬇️ 下载]          │
│                                                                 │
│  [🔄 执行转换]  [↔️ 交换方向]                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 三、页面结构设计

### 3.1 整体布局

```
┌─────────────────────────────────────────────────────────────────┐
│  🎨 Prompt Designer                      [用户头像] [设置] [?]  │
├─────────┬───────────────────────────────────────────────────────┤
│         │                                                       │
│  导航栏  │                      主内容区                         │
│         │                                                       │
│  ┌─────┐│                                                       │
│  │ 🏠  ││                                                       │
│  │首页 ││                                                       │
│  └─────┘│                                                       │
│  ┌─────┐│                                                       │
│  │ ✨  ││                                                       │
│  │新建 ││                                                       │
│  └─────┘│                                                       │
│  ┌─────┐│                                                       │
│  │ 📚  ││                                                       │
│  │仓库 ││                                                       │
│  └─────┘│                                                       │
│  ┌─────┐│                                                       │
│  │ 🔄  ││                                                       │
│  │转换 ││                                                       │
│  └─────┘│                                                       │
│  ┌─────┐│                                                       │
│  │ ⚙️  ││                                                       │
│  │设置 ││                                                       │
│  └─────┘│                                                       │
│         │                                                       │
├─────────┴───────────────────────────────────────────────────────┤
│  状态栏：模型：Claude 3.5 Sonnet | API 状态：✅ 已连接          │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 页面路由

| 路由 | 页面 | 功能 |
|------|------|------|
| `/` | 首页 | 快速入口、最近使用、统计概览 |
| `/create` | 新建提示词 | 向导式提示词生成流程 |
| `/create/cli` | CLI 专业版生成 | 生成 Anthropic XML 格式 |
| `/create/web` | Web 简明版生成 | 生成 Web 端格式 |
| `/library` | 提示词仓库 | 查看、管理已保存的提示词 |
| `/library/:id` | 提示词详情 | 查看、编辑、复制单个提示词 |
| `/convert` | 格式转换 | CLI ↔ Web 格式互转 |
| `/settings` | 设置 | API 配置、偏好设置 |

---

## 四、API 设计

### 4.1 RESTful API

#### 4.1.1 模型 API

```
# 模型配置
GET    /api/models              # 获取所有模型配置
POST   /api/models              # 添加模型配置
PUT    /api/models/:id          # 更新模型配置
DELETE /api/models/:id          # 删除模型配置
POST   /api/models/:id/test     # 测试模型连接

# AI 生成
POST   /api/generate            # 生成提示词（流式）
```

#### 4.1.2 提示词 API

```
# 提示词 CRUD
GET    /api/prompts             # 获取提示词列表（支持分页、筛选）
POST   /api/prompts             # 创建新提示词
GET    /api/prompts/:id         # 获取单个提示词
PUT    /api/prompts/:id         # 更新提示词
DELETE /api/prompts/:id         # 删除提示词

# 提示词操作
POST   /api/prompts/:id/convert # 转换格式
POST   /api/prompts/:id/copy    # 复制提示词
PATCH  /api/prompts/:id/favorite # 切换收藏状态

# 批量操作
POST   /api/prompts/export      # 导出提示词
POST   /api/prompts/import      # 导入提示词
```

#### 4.1.3 格式转换 API

```
POST   /api/convert/cli-to-web  # CLI → Web 转换
POST   /api/convert/web-to-cli  # Web → CLI 转换
```

### 4.2 请求/响应格式

```typescript
// 生成请求
interface GenerateRequest {
  userDescription: string;
  targetFormat: 'cli' | 'web' | 'both';
  language: 'zh' | 'en';
  modelId: string;
  options?: {
    enableThinking: boolean;
    enableExample: boolean;
    style: 'professional' | 'friendly' | 'academic';
  };
}

// 生成响应（流式）
interface GenerateResponse {
  type: 'analysis' | 'prompt' | 'complete' | 'error';
  content: string;
  metadata?: {
    roleIdentification?: string;
    recommendedTemplate?: string;
    suggestedTags?: string[];
  };
}

// 提示词响应
interface PromptResponse {
  id: string;
  name: string;
  description: string;
  category: string;
  tags: string[];
  cliVersion: string;
  webVersion: string;
  createdAt: string;
  updatedAt: string;
  usageCount: number;
  isFavorite: boolean;
}
```

---

## 五、数据存储方案

### 5.1 本地存储（IndexedDB）

适用于纯前端部署，无需后端。

```typescript
// 数据库结构
interface PromptDesignerDB {
  models: ModelConfig[];       // 模型配置
  prompts: StoredPrompt[];     // 提示词存储
  sessions: PromptDesignSession[]; // 生成会话
  settings: UserSettings;      // 用户设置
}

// 使用 Dexie.js 封装
import Dexie from 'dexie';

class PromptDesignerDatabase extends Dexie {
  models: Dexie.Table<ModelConfig, string>;
  prompts: Dexie.Table<StoredPrompt, string>;
  sessions: Dexie.Table<PromptDesignSession, string>;

  constructor() {
    super('PromptDesigner');
    this.version(1).stores({
      models: 'id, provider, isDefault',
      prompts: 'id, userId, category, *tags, createdAt, isFavorite',
      sessions: 'id, userId, createdAt'
    });
  }
}
```

### 5.2 云端存储（PostgreSQL）

适用于需要多设备同步的场景。

```sql
-- 模型配置表
CREATE TABLE models (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id),
  provider VARCHAR(50) NOT NULL,
  api_key_encrypted TEXT NOT NULL,
  model_id VARCHAR(100) NOT NULL,
  base_url TEXT,
  temperature DECIMAL(3,2) DEFAULT 0.7,
  max_tokens INTEGER DEFAULT 4096,
  is_default BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- 提示词表
CREATE TABLE prompts (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id),
  name VARCHAR(255) NOT NULL,
  description TEXT,
  category VARCHAR(50) NOT NULL,
  tags TEXT[] DEFAULT '{}',
  cli_version TEXT NOT NULL,
  web_version TEXT NOT NULL,
  usage_count INTEGER DEFAULT 0,
  is_favorite BOOLEAN DEFAULT FALSE,
  source_session UUID,
  model_used VARCHAR(100),
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- 创建索引
CREATE INDEX idx_prompts_user_id ON prompts(user_id);
CREATE INDEX idx_prompts_category ON prompts(category);
CREATE INDEX idx_prompts_tags ON prompts USING GIN(tags);
CREATE INDEX idx_prompts_created_at ON prompts(created_at DESC);
```

---

## 六、安全考虑

### 6.1 API Key 安全

```typescript
// 前端：仅传输加密后的 key
import { encrypt, decrypt } from './crypto';

async function saveApiKey(key: string): Promise<void> {
  const encrypted = await encrypt(key, getUserMasterKey());
  await db.models.update(modelId, { apiKeyEncrypted: encrypted });
}

async function getApiKey(modelId: string): Promise<string> {
  const model = await db.models.get(modelId);
  return decrypt(model.apiKeyEncrypted, getUserMasterKey());
}

// 后端：使用环境变量 + 加密存储
// - API Key 在传输时使用 HTTPS
// - 存储时使用 AES-256 加密
// - 主密钥从环境变量获取
```

### 6.2 输入验证

```typescript
// 防止 XSS
import DOMPurify from 'dompurify';

function sanitizeInput(input: string): string {
  return DOMPurify.sanitize(input, { ALLOWED_TAGS: [] });
}

// 验证提示词内容
function validatePrompt(prompt: string): ValidationResult {
  const maxLength = 50000; // 最大字符数

  if (prompt.length > maxLength) {
    return { valid: false, error: '提示词超过最大长度限制' };
  }

  // 检测潜在的注入攻击
  const dangerousPatterns = [
    /{{.*}}/,           // 模板注入
    /<script/i,         // XSS
  ];

  for (const pattern of dangerousPatterns) {
    if (pattern.test(prompt)) {
      return { valid: false, error: '检测到不安全内容' };
    }
  }

  return { valid: true };
}
```

---

## 七、开发计划

### Phase 1: 核心功能（MVP）

| 任务 | 优先级 | 依赖 |
|------|--------|------|
| 项目初始化（React + TypeScript + Tailwind） | P0 | - |
| UI 组件库搭建（shadcn/ui） | P0 | 项目初始化 |
| 模型 API 配置界面 | P0 | UI 组件库 |
| OpenAI/Anthropic API 集成 | P0 | 模型配置界面 |
| CLI 提示词生成流程（4 步向导） | P0 | API 集成 |
| 提示词仓库（本地存储） | P0 | CLI 生成 |
| 基础格式转换 | P1 | CLI 生成 |

### Phase 2: 完善功能

| 任务 | 优先级 | 依赖 |
|------|--------|------|
| Web 端提示词生成流程 | P1 | CLI 生成 |
| 一键格式切换 | P1 | 格式转换 |
| 提示词导入/导出 | P1 | 仓库 |
| 流式输出优化 | P1 | API 集成 |
| 深色模式 | P2 | UI 组件库 |
| 更多模型支持（Gemini, DeepSeek） | P2 | API 集成 |

### Phase 3: 高级功能

| 任务 | 优先级 | 依赖 |
|------|--------|------|
| 用户账户系统 | P2 | - |
| 云端同步 | P2 | 账户系统 |
| 提示词分享/社区 | P3 | 云端同步 |
| 提示词版本历史 | P3 | 云端同步 |
| 提示词效果测试 | P3 | API 集成 |

---

## 八、文件结构

```
prompt-designer/
├── public/
│   └── favicon.ico
├── src/
│   ├── components/              # 通用组件
│   │   ├── ui/                  # shadcn/ui 组件
│   │   ├── layout/              # 布局组件
│   │   │   ├── Header.tsx
│   │   │   ├── Sidebar.tsx
│   │   │   └── Layout.tsx
│   │   ├── prompt/              # 提示词相关组件
│   │   │   ├── PromptCard.tsx
│   │   │   ├── PromptEditor.tsx
│   │   │   ├── PromptPreview.tsx
│   │   │   └── TagSelector.tsx
│   │   └── model/               # 模型配置组件
│   │       ├── ModelSelector.tsx
│   │       └── ApiKeyInput.tsx
│   ├── pages/                   # 页面组件
│   │   ├── Home.tsx
│   │   ├── Create/
│   │   │   ├── index.tsx
│   │   │   ├── CliWizard.tsx
│   │   │   └── WebWizard.tsx
│   │   ├── Library/
│   │   │   ├── index.tsx
│   │   │   └── PromptDetail.tsx
│   │   ├── Convert.tsx
│   │   └── Settings.tsx
│   ├── hooks/                   # 自定义 Hooks
│   │   ├── useModels.ts
│   │   ├── usePrompts.ts
│   │   └── useGenerate.ts
│   ├── services/                # API 服务
│   │   ├── api/
│   │   │   ├── anthropic.ts
│   │   │   ├── openai.ts
│   │   │   └── index.ts
│   │   ├── convert.ts           # 格式转换服务
│   │   └── storage.ts           # 本地存储服务
│   ├── store/                   # 状态管理
│   │   ├── modelStore.ts
│   │   ├── promptStore.ts
│   │   └── settingsStore.ts
│   ├── types/                   # TypeScript 类型
│   │   ├── model.ts
│   │   ├── prompt.ts
│   │   └── index.ts
│   ├── utils/                   # 工具函数
│   │   ├── crypto.ts
│   │   ├── parser.ts
│   │   └── validators.ts
│   ├── constants/               # 常量
│   │   ├── templates.ts         # 提示词模板
│   │   └── rules.ts             # 转换规则
│   ├── App.tsx
│   ├── main.tsx
│   └── index.css
├── package.json
├── tsconfig.json
├── tailwind.config.js
├── vite.config.ts
└── README.md
```

---

## 九、参考资源

### 设计规范

- [CLAUDE.md](./CLAUDE.md) - Anthropic 官方提示词设计准则
- [Webrule.md](./Webrule.md) - Web 端提示词润色规则
- [Prompt.md](./Prompt.md) - 现有提示词示例

### 技术文档

- [Anthropic API](https://docs.anthropic.com/claude/reference)
- [OpenAI API](https://platform.openai.com/docs/api-reference)
- [shadcn/ui](https://ui.shadcn.com/)
- [Tailwind CSS](https://tailwindcss.com/docs)
- [Zustand](https://github.com/pmndrs/zustand)
- [Dexie.js](https://dexie.org/)

---

## 十、下一步行动

1. [ ] 确认技术栈选择（React vs Vue）
2. [ ] 确认部署方式（纯前端 vs 前后端分离）
3. [ ] 确认是否需要用户账户系统
4. [ ] 初始化项目结构
5. [ ] 开始 Phase 1 开发
